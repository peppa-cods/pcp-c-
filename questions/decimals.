#include <iostream>
#include <string>

using namespace std;
int main(){
	int n;
	cin >> n;
	float divisor = 2.0;
	float ans = 0.0;
	for (int i=2; i<n+2; i++){
		ans += 1.0/divisor;
		divisor += i*2;		
	}
	printf("%.2f",ans);
}


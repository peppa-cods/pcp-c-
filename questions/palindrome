#include <iostream>
#include <string>

using namespace std;

int main(){
	string str;
	cin >> str;
	if (str == string(str.rbegin(), str.rend())) {
    cout << str << " is a palindrome";
		} else{
			cout << str << " is not a palindrome";

		}
}


#include <iostream>
#include <stdio.h>
using namespace std;
struct prob10{
	int a, b;
	char c;
};
int main(){
	int n;
	cin >> n;
	prob10 a[n];
	for (int i=0; i<n; i++){
		scanf("%d%d %c", &a[i].a, &a[i].b, &a[i].c);
	}
	for (int i=0; i<n; i++){
		printf("%d %d %c\n", a[i].a, a[i].b, a[i].c);
	}


}